doctype html
html
  head
    title A Brief History of Time
    |
    link(rel='stylesheet', href='../../static/css/style.css')
  nav
    ul
      li
        a(href='all_books.html') Books
      |
      li
        a(href='all_patrons.html') Patrons
      |
      li
        a(href='all_loans.html') Loans
  body
    h1 Book: A Brief History of Time
    |
    form
      p
        label(for='title') Title
        input#title(type='text', value=loans[0].book.title)
      |
      p
        label(for='author') Author
        input#author(type='text', value=loans[0].book.author)
      |
      p
        label(for='genre') Genre
        input#genre(type='text', value=loans[0].book.genre)
      |
      p
        label(for='first_published') First Published
        input#first_published(type='text', value=loans[0].book.first_published)
      |
      p
        input(type='submit', value='Update')
    |
    h2 Loan History
    |
    table
      thead
        tr
          th Book
          |
          th Patron
          |
          th Loaned on
          |
          th Return by
          |
          th Returned on
          |
          th Action
      |
      tbody
        each loan in loans
            tr
              td
                a(href='book_detail.html') #{loan.book.title}
              |
              td
                a(href='patron_detail.html') #{loan.patron.first_name} #{loan.patron.last_name}
              |
              td #{loan.loaned_on}
              |
              td #{loan.return_by}
              |
              td #{loan.returned_on}
              |
              td
                a.button(href='return_book.html') Return Book
